{
  "name": "nodejs-week1",
  "version": "1.0.0",
  "description": "Express + Knex + SQLite project",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "start": "node api/index.js",
    "dev": "nodemon api/index.js",
    "migrate": "knex --knexfile ./api/db/knexfile.js migrate:latest",
    "rollback": "knex --knexfile ./api/db/knexfile.js migrate:rollback",
    "seed": "knex --knexfile ./api/db/knexfile.js seed:run",
    "migrate:make": "knex --knexfile ./api/db/knexfile.js migrate:make",
    "seed:make": "knex --knexfile ./api/db/knexfile.js seed:make",
    "db:reset": "npm run rollback && npm run migrate && npm run seed"
  },
  "dependencies": {
    "express": "^4.19.2",
    "knex": "^3.2.9",
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
